<div>
    {{-- @isset($recipeId) --}}
    {{-- {{$recipeId}} --}}
        {{-- @if($showFavorite=0)
          <button wire:click="addFavorite" >Add to Favorite</button>    
        @else
          <button wire:click="removeFavorite" >Remove from Favorite</button>
        @endif --}}
    {{-- @endisset --}}
    {{-- @isset($ingredientId)
        @if($showFavorite=0)
          <button wire:click="addFavorite" >Add to Favorite</button>    
        @else
          <button wire:click="removeFavorite" >Remove from Favorite</button>
        @endif
    @endisset --}}
</div>
